= simple_form_for [:admin, @location] do |f|
  = f.input :identifier
  = f.input :display_name
  = f.input :closed_stack

  => f.submit "Speichern", class: "btn btn-primary"
  = link_to "Abbrechen", admin_locations_path, class: "btn btn-default"
